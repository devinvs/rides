import "./CreateEvent.css";

function createEvent(name, location, time) {
    

    // send to api with fetch
}

export function CreateEventPage(props) {
    return (
        <div>
            <p>Hello the from Create Event Page</p>
        </div>
    )
}